stages:
  - Build homepage
  - Deploy

build:
  stage: Build homepage
  script:
    - cd Homepage
    - npm install
    - npm run build
  artifacts:
    paths:
      - ./Homepage/dist
      
deploy:
  stage: Deploy
  script: 
    - pm2 deploy ecosystem.config.js dev
  dependencies:
    - build
